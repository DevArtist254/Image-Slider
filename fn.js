//Max out start heading back
maxSlide = getAllSlide.length + 1

//When sliding from the start
0 = getAllSlide.length

Tracking every iteration of the slide

slides ["1","2","3","4","5","6"]

function left() {
 every click moves the indcator forward till the end of list then start the count 
if (not(getAllSlide.length + 1)) {
i++    
}else {
reset it to zero
}
}

function right() {
 every click moves the indcator backwards till the end of list then start the count 
if (not(0 = getAllSlide.length)) {
i--    
}else() {
reset to the max
}
}